<h1>All Messages</h1>
<p>© <%= Date.today.year %> Let`s Talk About ...</p>
<% if user_signed_in? %>
  <p><%= current_user.email %></p>
<% end %>
<br />
<div class="col-md-6 col-md-offset-2">
    <% @messages.each do |message| %>
    <h3><%= message.title %></h3>
    <br />
    <%= link_to "Detals", message_path(message), class: "btn btn-default"  %>
    <% end %>
    <br /><hr />
    <%= link_to "New Message", new_message_path %>
</div>
